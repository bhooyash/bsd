#include <iostream>
#include <math.h>
int main() {
	double x=3.89;
	double y= 4;
	double z;
	//z= std::max(x,y);(these are some of the basic functions)
	//z= std::min(x,y);
	//z= pow(2,4);
	//z = abs(-3);
	//z = round(x);
	//z = ceil(x);
	//z = floor(x);
	std::cout <<z;
	return 0;
}
